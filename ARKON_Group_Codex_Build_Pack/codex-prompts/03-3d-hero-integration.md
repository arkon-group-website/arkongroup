# Codex Task 03 — 3D hero integration

Read docs/3d-hero-concept.md.

Improve `arkon-3d-loader.js` and the hero section so it supports:
- Spline URL injection
- fallback image on mobile
- reduced-motion users
- save-data users
- lazy loading
- accessibility title/alt text

Do not load a large 3D scene on mobile by default.
